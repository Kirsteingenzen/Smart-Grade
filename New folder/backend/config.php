<?php
// Database connection parameters for smartgrade
$host = "localhost";
$username = "root";
$password = "";
$dbname = "smartgrade";

// You can optionally keep the $conn object here if other files use it,
// but student-management.php needs the individual variables.
/*
$conn = new mysqli($host, $username, $password, $dbname);
if ($conn->connect_error) {
    die("Connection failed: " . $conn->connect_error);
}
*/
?>